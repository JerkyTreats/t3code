diff --git a/dist/BunHttpServer.js b/dist/BunHttpServer.js
index 5fb0cbacc183b58e31a442badf4aef06bebcd592..931f5678ffd3b1518d99a4498571f716f077ce89 100644
--- a/dist/BunHttpServer.js
+++ b/dist/BunHttpServer.js
@@ -45,6 +45,10 @@ export const make = /*#__PURE__*/Effect.fnUntraced(function* (options) {
     ...options,
     fetch: handlerStack[0],
     websocket: {
+      perMessageDeflate: {
+        compress: "shared",
+        decompress: "shared"
+      },
       open(ws) {
         Deferred.doneUnsafe(ws.data.deferred, Exit.succeed(ws));
       },
@@ -355,11 +359,11 @@ class BunServerRequest extends Inspectable.Class {
       resume(Effect.map(Deferred.await(deferred), ws => {
         const write = chunk => Effect.sync(() => {
           if (typeof chunk === "string") {
-            ws.sendText(chunk);
+            ws.sendText(chunk, true);
           } else if (Socket.isCloseEvent(chunk)) {
             ws.close(chunk.code, chunk.reason);
           } else {
-            ws.sendBinary(chunk);
+            ws.sendBinary(chunk, true);
           }
           return true;
         });
diff --git a/src/BunHttpServer.ts b/src/BunHttpServer.ts
index 91bb25fd1793bd1edc721cefad98bbdeb6cf8877..28ccab9e2f9a098c280002671257c57924106890 100644
--- a/src/BunHttpServer.ts
+++ b/src/BunHttpServer.ts
@@ -111,6 +111,10 @@ export const make = Effect.fnUntraced(
       ...options as ServeOptions<R>,
       fetch: handlerStack[0],
       websocket: {
+        perMessageDeflate: {
+          compress: "shared",
+          decompress: "shared"
+        },
         open(ws) {
           Deferred.doneUnsafe(ws.data.deferred, Exit.succeed(ws))
         },
@@ -561,11 +565,11 @@ class BunServerRequest extends Inspectable.Class implements ServerRequest.HttpSe
         const write = (chunk: Uint8Array | string | Socket.CloseEvent) =>
           Effect.sync(() => {
             if (typeof chunk === "string") {
-              ws.sendText(chunk)
+              ws.sendText(chunk, true)
             } else if (Socket.isCloseEvent(chunk)) {
               ws.close(chunk.code, chunk.reason)
             } else {
-              ws.sendBinary(chunk)
+              ws.sendBinary(chunk, true)
             }

             return true
