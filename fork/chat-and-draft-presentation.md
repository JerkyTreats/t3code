# Ordinary Chat And Draft Presentation

Date: 2026-09-05
Status: active

The retained visual contract is ordinary chat and draft presentation captured from accepted origin. Current upstream provides the message hierarchy, centered draft hero, composer width, plan card, ordinary Markdown, generic attachments, draft status and sends. These shared outcomes remain part of product preservation without claiming fork isolation credit.

`apps/web/src/fork/chatComposerPresentation.tsx` owns floating desktop permission-control placement, existing runtime callbacks, resting action padding and 24-pixel composer geometry. `chatPresentation.ts` owns conversation top spacing independently of upstream titlebar fade depth. `chatPresentation.css` joins the full-width checkout strip to one continuous glass backdrop. Current ChatComposer, ChatView and MessagesTimeline consume those choices through rendering adapters.

The runtime mode list, provider availability, approval and pending-input gates remain current upstream behavior. The floating control consumes the existing callback and does not invent a permission mode. The generic attachment button, prompt stash, draft indicator and upstream task/review controls remain. F24 queue shelf, F09 document expansion and F21 rendered-document comments stay retired.

Mounted composer tests exercise runtime callback preservation and floating placement. Synthetic conversation and empty/editable draft cases cover light and dark surfaces, plan presentation, message hierarchy, composer corners and checkout width at the same viewport as the accepted reference. The intake ledger records final candidate and visual evidence; source classes alone do not establish visual acceptance. Later compact T3 Thread consumption remains a separate acceptance obligation.
